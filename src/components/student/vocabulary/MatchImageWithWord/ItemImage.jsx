import { styled } from "@mui/material/styles";

const ItemImage = styled("img")({
  width: "100%",
  height: "100%",
  borderRadius: "0.5rem",
  aspectRatio: "3 / 2",
  objectFit: "cover",
});

export default ItemImage;
